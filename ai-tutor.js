export default async function handler(req,res){if(req.method!=='POST')return res.status(405).json({error:'Method not allowed'});if(!process.env.AI_API_KEY)return res.json({reply:'Demo tutor: break the problem into a mental model, a small experiment, and a measurable checkpoint.'});const r=await fetch(process.env.AI_API_URL||'https://api.openai.com/v1/chat/completions',{method:'POST',headers:{'Content-Type':'application/json',Authorization:`Bearer ${process.env.AI_API_KEY}`},body:JSON.stringify({model:process.env.AI_MODEL,messages:[{role:'system',content:'You are the AR.com practical AI learning tutor. Teach clearly and guide learners to build.'},...(req.body?.messages||[])]})});const d=await r.json();res.status(r.ok?200:502).json({reply:d.choices?.[0]?.message?.content||'No response'});}